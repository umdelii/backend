jpa 프로젝트 생성
- spring web, lombok, thymeleaf, dev tools, validation
- spring jpa, mysql

데이터베이스 연동
자바 + DB연동 => JDBC 
여지껏 순수 연동시켜 insert,delete...했었다

데이터베이스 작업을 쉽게 도와주는 라이브러리가 있다! 
1. MyBatis
2. ORM(Object Relational Mapping)
    테이블 행 하나 == 객체 (개념)
    --> ORM개념을 실제로 기술로 만들어낸것이 Hibernate
    --> JPA(Java Persistence API) : java라는 언어에서 DB관련한 API

entity 폴더: db 테이블과 연동하는 객체가 들어있는 폴더
repository : 

jpa entity 작성 시 불러오는 어노테이션
@Entity == 이 클래스는 테이블과 연동되어 있음 
    -> @Id 필수~
    db의 create table과 같은 의미 
@Id = pk(primary key)
@Column(~~) = 내가 임의로 db 테이블안 컬럼의 옵션들을 지정할때 사용

spring-boot-starter-test : 어플리케이션 테스트 라이브러리 포함
 - Junit

@springBootTest : spring boot test 클래스야
테스트 클래스 내부에 작성하는 메소드 위에 무조건 @Test 붙여야함

@AutoWired
private StudentRepository StudentRepository

스프링(부트) 프레임워크
 => 개발자의 할 일 축소
 => 객체 생성 => 사용
    A obj = new A();
    obj.method();
 => 스프링이 대신 new해주니 나는 new 해야 할 객체를 알려주기(어노테이션)
    @Controller, @Service, @Repository, @Component, ...
 => 주입이 필요함 : 그게 바로 @AutoWired

웹어플리케이션
웹서버 + WAS(컨테이너(tomcat))

tomcat 아래 지정된 폴더에 작성된 프로젝트 배포(.jar)

프로그램이 돌아가는 ex. 코드수정을 해 -> 톰갯이 리로드해줘 -> gradle(build)이 배포(코드 update해줘,class jar로 묶어워등등 작업해주고해줘) 해줘 -> 실행 해줘
./gradlew build


Entity 동작 확인!
1. Repository 작성(interface 클래스명 extends JpaRepository)
2. test 코드 작성 (test폴더에)

스프링 컨테이너
bean(객체)관리

junit => java test해주는 제일 유명한 test 라이브러리




유용한 사이트
1. 필요한 라이브러리(리파지토리) 가져오기
https://mvnrepository.com/ 
2. db 쿼리문을 실행해줄수있게하는 jpa 문
https://docs.spring.io/spring-data/jpa/reference/jpa/query-methods.html



------------------
연관관계 
db의 외래키 개념

1. 설정
 - 1:1 => @OneToONe
    ex. 회원 : 프로필 
 - 1:N => @OneToMany /@ManyToOne
    ex. 대부분의 업무
        게시글 : 댓글
        자료 : 첨부파일
        회원 : 멀티프로필 
 - M:N => @ManyToMany
    sql 코드 상으로 불가능 => 1:N으로 쪼갬

@ManytoOne(optional = true) -> null 허용하는 조일컬럼이 생성됨
    허용하지않으려면 false
    join ~ on ~ : 자동으로 join 처리

@ManyToOne(optional = false, fetch = FetchType.LAZY)
 - 외래 키 테이블 정보를 조회하지 않음 => join하지 않음
 - 외래 키에 해당하는 객체 탐색 시 => LazyInitializationException 오류가 뜬다


@JoinColumn(name = "")
 - 따로 name 설정안하면? 부모키가 team_id면 team_team_id 이렇게 만들어줌 ...
 - 테이블명_기본키명

@OnetoMany 
 - @ManyToOne의 상대방 클래스에 작성
 - 왜 걸까?
    원 sql의 외래키 개념과 불균형이 발생됨(객체에서는 양쪽에서 접근이 불가능하다) => 해결하기 위해 양쪽에 객체 관계를 설정해서 jpa에 알려줘야해
 - 조금 문제가 있다... what?
    얘는 fetch가 기본 LAZY기때문에 조인구문으로 처리하지않음
FetchType.LAZY(지연로딩) => 본인 테이블의 정보만 먼저 가져오기때문에 조인관계 테이블에 접근시 LazyInitializationException이 뜬다
    해결법 1. 해당 메소드에 @Transactional을 붙이기
        but, 본인, 조인관계 테이블 접근 시 각각의 select 구문으로 처리(select 두번함) (이게 더 처리속도가 빠른 경우도 있어)
    해결법 2. left join으로 처리하게 하자(FetchType.EAGER(즉시로딩))

@OnetoOne : 기본적으로 내부조인으로 처리

그래서 기본적으로 @ManyToOne이나 @OneToOne은 -> FetchType.LAZY 처리 => @Transactional 하거나 / @EntityGraph(필요할때만 join 구문 사용) 한다

@OnetoOne(mappedBy = "부모키있는테이블변수명")
 = 외부조인으로 처리

객체 그래프 탐색
FetchType.EAGER(즉시로딩) : 연관된 객체를 조회하는 시점에 바로 사용
FetchType.LAZY(지연로딩) : 연관된(외래키) 객체를 실제 사용하는 시점에 데이터베이스에서 조회

1. 단방향(@ManytoOne)
    - 내부조인으로 처리
    - fetch = FetchType.LAZY를 통해 해당 테이블 정보만 조회 
        => 해결 1. @Transactional
        => 해결 2. @EntityGraph
2. 양방향(@ManytoOne/@OnetoMany)
    - @OnetoMany : default 값이 LAZY다
3. 단방향(@OnetoOne)
    - @ManyToOne 이랑 똑같다.
4. 양방향(@OneToOne/@OneToOnej)
    - 외부조인하네? left join

영속성 관리
 => 내부적으로 Persistence Context를 이용해 관리함

Persistence Context
 1차 캐시 : 조회한 엔티티를 저장, 같은 엔티티 조회 시 재쿼리 실행 방지
 변경 감지 : dirty check  (값 변경 시 update 구문 실행) 
 엔티티 동일성 보장 / 라이프 사이클(New,Managed,Removed,Detached) 관리
 지연 로딩(Lazy Loading) = 프록시 유지

 부모 / 자식
 @OneToMany(mappedBy = "부모변수명", cascade = {CascadeType.PERSISIT, CascadeType.REMOVE}, orphanRemoval = true)
    cascade = 하나의 엔티티에 발생하는 과정을 다른 엔티티에도 전파
    CascadeType.PERSISIT : 부모 삽입 시 자식도 삽입
        코드 상은 ArrayList에 자식 추가 => 부모만 save()
    CascadeType.REMOVE : 부모 삭제 시 자식도 삭제
        코드 상은 부모만 제거 
    orphanRemoval : 고아객체
        코드 상 부모 엔티티 컬렉션에서 자식 엔티티의 참조만 제거 => 부모만 save()
        p.getChilds().remove();